extends layout

block content
  div.wrapper  
    div.container
      h1 Welcome to the message board  
      button.btn(click='/new')  Add a new message
      each message in messages
        ul 
          li= message.text 
          li= message.user 
          li= message.added

